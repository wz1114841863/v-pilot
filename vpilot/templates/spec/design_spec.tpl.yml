# vpilot/templates/spec/design_spec.tpl.yml
module_name: "" # 例如: "pipelined_multiplier"
description: "" # 详细描述该模块的设计

# v-pilot 将根据此字段, 选择 'sequential' 骨架或 'combinational' 骨架
design_type: "" # 填充 'sequential' 或 'combinational'

# v-pilot 将用此信息填充 'base_bfm.py' 中的 BFM_HANDLES
key_signals:
  clk: ""   # 时钟信号名称, e.g., "clk_i", 或置空""
  rst_n: "" # 复位信号名称, e.g., "rst_n_i", 或置空""

parameters:
  - name: "DATA_WIDTH"
    type: "integer"
    default_value: 8
    description: "输入操作数和输出结果的数据位宽"
  # - name: ...

ports:
  - name: "clk"
    direction: "input"
    width: 1
    description: "系统时钟"
  - name: "rst_n"
    direction: "input"
    width: 1
    description: "异步复位,低有效"
  # - name: ...

key_features:
  - "功能点1..."
  - "功能点2..."
  # 例如: "4级流水线结构"
  # 例如: "支持旁路逻辑以减少数据冒险"

assumptions_and_constraints:
  - "假设1..."
  - "约束1..."
  # 例如: "复位信号rst_n是异步的"
